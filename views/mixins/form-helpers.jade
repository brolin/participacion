mixin label(field, title)
  label(for=field, class="control-label")= title

mixin password(field, value)
  input.span6(type="password", id=field, name=field, value=value)

mixin text(field, value)
  input.span6(type="text", id=field, name=field, value=value)

mixin textarea(field, value)
  textarea.span6(id=field, name=field, rows="5")= value

mixin number(field, value)
  input.span2(type="number", id=field, name=field, value=value)

mixin boolean(field, value)
  input.span1(type="checkbox", id=field, name=field, checked=value)

mixin date(field, value)
  input.span2(type="date", id=field, name=field, value=dateAsValue(value))

mixin datetime(field, value)
  input.span2(type="datetime", id=field, name=field, value=value)

mixin selectMultiple(field, value, options)
  -var options= options
  - console.log(value)
  -var valores = value.toUpperCase().split(",")
  select.selectpicker.span6(id=field, name=field, value= value, multiple, data-live-search="true", data-container="body")
    - each opcion in options
      - console.log(JSON.stringify(valores))
      - if(valores.indexOf(opcion) > -1)
        option(selected="true") #{opcion}
      - else
        option #{opcion}

mixin select(field, value, options)
  -var options= options
  - console.log(value)
  -var valores = value.toUpperCase().split(",")
  select.selectpicker.span2(id=field, name=field, value= value, data-live-search="true", data-container="body")
    - each opcion in options
      - if(valores.indexOf(opcion) > -1)
        option(selected="true") #{opcion}
      - else
        option #{opcion}

mixin tagsInput(field, value)
  input.span6(type="text", name= field, value= value, data-role="tagsinput", placeholder="Agregue el nombre y presione enter")

mixin errorMessage(model, field)
  !{errorFor(model, field)}
